/*import * as CloudUI from 'cloud-ui.vusion/theme-seagreen';

export default {
    install(Vue) {
        // for (const key in directives)
        //     Vue.directive(key, directives[key]);
        for (const key in CloudUI)
            Vue.component(CloudUI[key].name, CloudUI[key]);
    },
};
*/
